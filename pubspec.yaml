name: we_are_ready
description: "WeAreReady — disaster-response volunteer matching app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  cloud_firestore: ^6.4.1
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.2
  flutter_riverpod: ^2.6.1
  go_router: ^14.3.0
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
